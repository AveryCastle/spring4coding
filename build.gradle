apply plugin: 'java'

group = 'com.apress.isf.java'
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile("org.codehaus.groovy:groovy-all:2.4.+")
    compile("org.springframework:spring-context:4.2.2.RELEASE")
    testCompile("junit:junit:4.12")
}

test {
    testLogging {
        events 'started', 'passed'
    }
}
